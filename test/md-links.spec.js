const mdLinks = require('../index');
// const { transformToAbsolutePath, checkIfPathExists, checkPathExtension } =  require('../components/pathAnalysis');
// const { readFiles } = require('./components/mdLinks');

// const readFiles = require('../components/mdLinks');

// const absolutePath = 'C:\Users\Juan Viloria\Desktop\LABORATORIA\BOOTCAMP\4to proyecto\DEV010-md-links\prueba.md';

/*describe('mdLinks', () => {
  it('debería resolver con un array de enlaces', () => {
    const receivedPath = './prueba.md';;
    return mdLinks(receivedPath)
      .then((links) => {
        expect(Array.isArray(links)).toBe(true);
      });
  }); 

});*/

/*describe('mdLinks', () => {
  it('should return an array with 3 links for an .md file with 3 links', (done) => {
    mdLinks('./prueba.md')
    .then((links) => {
      expect(links.length).toBe(3);
      done(); // Llama a done() para indicar que la prueba ha finalizado
    })
    .catch((error) => {
      done(); // Llama a done(error) si ocurre un error durante la prueba
    });
});
});*/